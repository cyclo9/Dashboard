/** @type {import('next').NextConfig} */
const nextConfig = {
	// code snippet provided by https://github.com/gregrickaby/nextjs-github-pages
	output: "export",
	basePath: "/nextjs-github-pages",
	images: {
		unoptimized: true,
	},
}

module.exports = nextConfig
